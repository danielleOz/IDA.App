<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:IDA.App.ViewModels"
             xmlns:models="clr-namespace:IDA.App.Models"
             x:DataType="viewmodels:JobOfferPageViewModels"
             x:Class="IDA.App.Views.JobOfferPage">
    <ContentPage.Resources>
        <Style TargetType="Grid">
            <Setter Property="VisualStateManager.VisualStateGroups">
                <VisualStateGroupList>
                    <VisualStateGroup x:Name="CommonStates">
                        <VisualState x:Name="Normal" />
                        <VisualState x:Name="Selected">
                            <VisualState.Setters>
                                <Setter Property="BackgroundColor"
                                        Value="LightSkyBlue" />
                            </VisualState.Setters>
                        </VisualState>
                    </VisualStateGroup>
                </VisualStateGroupList>
            </Setter>
        </Style>

    </ContentPage.Resources>
    <ContentPage.Content>


        <StackLayout BackgroundColor="#fff7f7">
            <Label Text=""></Label>
            <Frame BorderColor="#aaaaaa" BackgroundColor="#fff7f7" CornerRadius="6" HorizontalOptions="Center" WidthRequest="350" HeightRequest="350">
                <StackLayout >
                    <SearchBar Placeholder="search services" Text="{Binding Services}"/>
                    <CollectionView EmptyView=""  x:Name="services" ItemsSource="{Binding FilteredServices}" SelectedItem="{Binding SelectedService}" SelectionMode="Single"
                                    SelectionChangedCommand="{Binding SelectServicesCommand}" SelectionChangedCommandParameter="{Binding SelectedService}">
                                   <!--IsVisible="{Binding ShowServicesError}"--> 
                    </CollectionView>


                    <Button Command="{Binding Path=SearchCommand}"></Button>
                </StackLayout>

            </Frame>

            <StackLayout IsVisible="{Binding IsSearched}">
            <Frame BorderColor="#aaaaaa" BackgroundColor="#fff7f7" CornerRadius="6" HorizontalOptions="Center" WidthRequest="350" HeightRequest="150">
                <StackLayout >
                    <StackLayout >

                        <CollectionView  Grid.Row="1" SelectionMode="Single" SelectedItem="{Binding WorkerId, Mode=TwoWay}"  ItemsSource="{Binding Workers}" EmptyView="">
                            <CollectionView.ItemTemplate>
                                <DataTemplate x:DataType="models:Worker">
                                    
                                    <Grid RowSpacing="0" ColumnSpacing="0" Margin="0,15,0,15">
                                        
                                        
                                    <Label VerticalTextAlignment="Center" VerticalOptions="Center">
                                        <Label.FormattedText>
                                            <FormattedString>
                                                <Span Text="{Binding FirstName} " />
                                                    <Span Text=" "></Span>
                                                <Span Text="{Binding LastName} " />
                                            </FormattedString>
                                        </Label.FormattedText>
                                    </Label>
                                        
                                    <Grid.RowDefinitions>
                                            <RowDefinition Height="50"></RowDefinition>
                                            <RowDefinition Height="2*" ></RowDefinition>
                                            <RowDefinition Height="*"></RowDefinition>
                                        </Grid.RowDefinitions>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="500" ></ColumnDefinition>
                                            <ColumnDefinition Width="*" ></ColumnDefinition>
                                        </Grid.ColumnDefinitions>

                                    </Grid>
                                    
                                   
                                </DataTemplate>
                            </CollectionView.ItemTemplate>
                        </CollectionView>

                </StackLayout>

                    
                </StackLayout>

            </Frame>
            </StackLayout>

            <Button Text="see this workers profile" BackgroundColor="#f0d9d7" TextColor="White" CornerRadius="3"  Command="{Binding Path=WorkerPCommand}"></Button>

            <StackLayout IsVisible="{Binding IsWorker}">
                <Button Text="availbilty update" BackgroundColor="#f0d9d7" TextColor="White" CornerRadius="3" Command="{Binding UpdateAvailbiltyCommand }"></Button>

            </StackLayout>

        </StackLayout>
    </ContentPage.Content>
</ContentPage>
    