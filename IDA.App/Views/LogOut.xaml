<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local ="clr-namespace:IDA.App.ViewModels"
             x:Class="IDA.App.Views.LogOut">
    <ContentPage.BindingContext>
        <local:LogOutViewModels></local:LogOutViewModels>
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <StackLayout>
            <Label Text="do you want to log out?" VerticalOptions="CenterAndExpand"    HorizontalOptions="CenterAndExpand" />
            <Button Text="log Out" Command="{Binding Path=LogOutCommand}" > </Button>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>